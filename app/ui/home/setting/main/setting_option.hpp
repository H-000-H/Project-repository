#ifndef SETTING_OPTION_HPP
#define SETTING_OPTION_HPP
/*设置选项的封装*/
#include "lvgl.h"

namespace ui
{
    // 设置选项
    class Option
    {
    public:
/* ------------------------------------------------------------------------------------------------------------------------------------------------ */
/* ------------------------------------------------------------------------------------------------------------------------------------------------ */
        Option();
        ~Option();

    protected:
/* ------------------------------------------------------------------------------------------------------------------------------------------------ */
/* ------------------------------------------------------------------------------------------------------------------------------------------------ */
        /**
         * @brief 创建选项
         * @param[in] parent lv_obj_t* 挂载父对象
         * @return bool 是否成功
         */
        bool create_option(lv_obj_t*parent);

        // 设置选项尺寸
        void set_option_size();

        // 设置选项颜色
        void set_option_color();
    };
}

#endif // SETTING_OPTION_HPP
